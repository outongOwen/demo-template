import { defineStore } from 'pinia'
import { {{upperDataName}}StateType } from './types'

export const use{{upperDataName}}Store = defineStore('use{{upperDataName}}Store',{
  state: (): {{upperDataName}}StateType => ({}),
  getters: {},
  actions: {}
})
